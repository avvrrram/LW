count = 0

print("Число", " " * 4, "Кратне трьом")

# Проходимо по діапазону від 30 до 60
for i in range(30, 61):
    # Якщо число кратне трьом
    if i % 3 == 0:
        count += 1
        print(i, " " * 6, i)

print("Кількість чисел, кратних трьом:", count)
