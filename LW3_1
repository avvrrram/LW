t = str(input("Введіть слово, яке не перевищує 8 символів: "))

# Перевіряємо, чи довжина слова не перевищує 8 символів
while len(t) > 8:
    t = str(input("Введіть ще раз слово, так як воно перевищує 8 символів: "))

# Виводимо різні зрізи, якщо довжина слова достатня для виконання операції зрізу
if len(t) >= 1:
    print("Після зрізу першого символу та останього: ", t[1:len(t) - 1])

if len(t) >= 3:
    print("Перші три символи: ", t[:3])

if len(t) >= 5:
    print("Останні три символи: ", t[-3:])

if len(t) >= 4:
    print("Символи з індексу 2 до 4 (включно): ", t[2:5])

if len(t) >= 2:
    print("Кожен другий символ: ", t[::2])

# Виводимо увесь рядок від початку до кінця
print("Увесь рядок: ", t)
